/*
ADT SortedMultiMap – using a SLLA with unique keys ordered based on a relation on the keys. 
Every key will be associated with a SLLA of the values belonging to that key
*/
#include "ShortTest.h"
#include "ExtendedTest.h"
int main() {
	testAll();
	testAllExtended();
}